import { tv } from 'tailwind-variants';

export const kbdStyle = tv({
  base: 'bg-background-100 border border-outline-200 rounded px-1.5 py-0.5 flex-row items-center',
  variants: {},
});

export const kbdTextStyle = tv({
  base: 'text-xs font-mono text-typography-700',
  variants: {},
});
