import { tv } from 'tailwind-variants';

export const blockquoteStyle = tv({
  base: 'border-l-4 border-primary-300 pl-4 py-2',
  variants: {},
});

export const blockquoteTextStyle = tv({
  base: 'text-base italic text-typography-600',
  variants: {},
});
