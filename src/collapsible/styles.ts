import { tv } from 'tailwind-variants';

export const collapsibleStyle = tv({ base: 'w-full', variants: {} });
export const collapsibleTriggerStyle = tv({ base: 'w-full', variants: {} });
export const collapsibleContentStyle = tv({ base: 'overflow-hidden', variants: {} });
