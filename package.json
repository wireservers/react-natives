{
  "name": "wireservers-ui",
  "private": true,
  "workspaces": [
    "website",
    "packages/*"
  ],
  "scripts": {
    "build": "cd website && npx expo export --platform web",
    "start": "node server.js"
  }
}
