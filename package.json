{
  "name": "wireservers-ui",
  "private": true,
  "workspaces": [
    "demos/expo",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=demos/expo",
    "start": "node server.js"
  }
}
